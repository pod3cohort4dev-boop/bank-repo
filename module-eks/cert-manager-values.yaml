# cert-manager-values.yaml
installCRDs: false  # CRDs already exist

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

startupapicheck:
  enabled: true
  timeout: 5m
  extraArgs:
    - --timeout=5m

webhook:
  timeoutSeconds: 30